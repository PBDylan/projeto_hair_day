import { schedulesDay } from "./schedules/load.js";
// Importante: o código deve ser executado após o carregamento do DOM

document.addEventListener("DOMContentLoaded", function () {
  schedulesDay();
});
